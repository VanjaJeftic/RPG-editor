//$('.last a')[0].click();

/*if($(document).text().includes('Last »') === true){
    let max = 0;
    $("tr").each(function() {
        let row_value = this.id;
        let new_row_value = row_value.slice(5);
        max = Math.max(new_row_value, max);
    });
    let output = "type_" + max;

    $( ".last a" )[0].click();
}*/
$('#types_table').append('<%= j render partial: "type", locals: {type: @type} %>');
location.reload();


$( 'tr' ).last().addClass("highlight");
setTimeout(function() {
    $( "tr" ).removeClass('highlight');
}, 4000);






