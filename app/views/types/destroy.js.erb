$('.delete').bind('ajax:success', function(){
   $(this).closest('tr').fadeOut();
});

var url = window.location.href;     // Returns full URL (https://example.com/path/example.html)

if(url == 'http://localhost:3000/types' || url == 'http://localhost:3000/types?show_all=false' ||
url == 'https://serene-caverns-52637.herokuapp.com/types?show_all=false' || url == 'https://serene-caverns-52637.herokuapp.com/types'){
    $.ajax({url: "/types", type: "GET"})
}
